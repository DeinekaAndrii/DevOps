#!/bin/bash

FILE="/etc/passwd"

if [ -n "$1" ]; then
  FILE="$1"
fi

if [ ! -f "$FILE" ]; then
  echo "Error: File '$FILE' not found." >&2
  exit 1
fi

awk -F: '$7 == "/bin/bash" { print $1 }' "$FILE"
